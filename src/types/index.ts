// Global, generic TypeScript interfaces and types shared across the entire app.
// Domain-specific types should live next to their feature (see `src/features/*`)
// instead of being dumped in here. This file is intentionally left empty for now.

export {};
