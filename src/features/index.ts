// Root barrel for `src/features`.
//
// This directory is the home for distinct business domains, following a
// simplified Feature-Sliced Design (FSD) approach. Each domain owns its own
// components, hooks, services, and types, e.g.:
//   - features/auth-flow        (sign in, sign up, password reset)
//   - features/internship-wizard (multi-step internship application flow)
//   - features/grading          (grading/evaluation domain)
//
// Cross-feature, generic building blocks should stay out of here and live in
// `src/components`, `src/hooks`, or `src/services` instead.

export {};
