'use client';

/**
 * Product spinner — thin facade over `ui/spinner`.
 * Features/app must import this, not `@/components/ui/spinner`.
 */
export { Spinner as KvSpinner } from '@/components/ui/spinner';
