import { ProfileRoutePlaceholder } from '@/features/shared/profile/components/ProfileContainer';

/**
 * Route-level loading for profile — plain placeholder (no skeleton UI).
 */
export default function ProfileLoading() {
  return <ProfileRoutePlaceholder />;
}
